import L from "leaflet";
import type { ElevationMode } from "../types/elevationMode";
export declare type PopupOptions = L.PopupOptions & {
    elevation?: number;
    elevationMode?: ElevationMode;
    closeWhenMovedOffscreen?: boolean;
};
declare class PopupType extends L.Popup {
    options: PopupOptions;
    constructor(options?: PopupOptions, source?: L.Layer);
    getElevation(): number;
    setElevation(elevation: number): this;
}
export declare type Popup = PopupType;
export declare const Popup: typeof PopupType;
export declare const popup: import("../private/factoryFor").Constructor<typeof PopupType>;
export {};
