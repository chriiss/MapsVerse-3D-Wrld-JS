import L from "leaflet";
export declare class Vector3 {
    x: number;
    y: number;
    z: number;
    constructor(vector: Vector3);
    constructor(vector: [number, number, number]);
    constructor(x: number);
    constructor(x: number, y: number);
    constructor(x: number, y: number, z: number);
    toPoint(): L.Point;
}
export declare class Vector4 {
    x: number;
    y: number;
    z: number;
    w: number;
    constructor(vector: Vector4);
    constructor(vector: [number, number, number?, number?]);
    constructor(x: number);
    constructor(x: number, y: number);
    constructor(x: number, y: number, z: number);
    constructor(x: number, y: number, z: number, w: number);
}
export declare const zoomToDistance: (zoom: number) => number;
export declare const distanceToZoom: (distance: number) => number;
