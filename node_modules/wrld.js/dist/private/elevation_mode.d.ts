import { ElevationMode } from "../types/elevationMode";
export declare enum ElevationModeType {
    HEIGHT_ABOVE_GROUND = "heightAboveGround",
    HEIGHT_ABOVE_SEA_LEVEL = "heightAboveSeaLevel"
}
export declare const isValidElevationMode: (elevationMode: ElevationModeType | ElevationMode) => boolean;
export declare const getElevationModeInt: (elevationModeString: ElevationModeType) => number;
