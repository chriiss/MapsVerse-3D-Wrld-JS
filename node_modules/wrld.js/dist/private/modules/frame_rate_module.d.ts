export function FrameRateModule(emscriptenApi: any, targetVSyncInterval: any, throttledTargetFrameIntervalMilliseconds: any, idleSecondsBeforeThrottle: any, throttleWhenIdleEnabled: any): void;
export class FrameRateModule {
    constructor(emscriptenApi: any, targetVSyncInterval: any, throttledTargetFrameIntervalMilliseconds: any, idleSecondsBeforeThrottle: any, throttleWhenIdleEnabled: any);
    onInitialized: () => void;
    ready: () => boolean;
    setTargetVSyncInterval: (targetVSyncInterval: any) => void;
    setThrottledTargetFrameInterval: (throttledTargetFrameIntervalMilliseconds: any) => void;
    setIdleSecondsBeforeThrottle: (idleSecondsBeforeThrottle: any) => void;
    setThrottleWhenIdleEnabled: (throttleWhenIdleEnabled: any) => void;
    cancelThrottle: () => void;
}
export default FrameRateModule;
